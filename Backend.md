BACKEND
________________________________________
🧠 Элина — Backend Architect (КЛЮЧЕВОЙ)
Делает:
•	создаёт Django-проект 
•	настраивает PostgreSQL 
•	создаёт ВСЕ модели: 
o	User 
o	Tour 
o	Booking 
o	Payment 
o	Review 
•	настраивает связи (ForeignKey) 
•	делает миграции 
•	настраивает JWT (auth) 
Отдаёт:
•	готовые модели 
•	базовую auth систему 
•	структуру API (без полной логики) 
НЕ делает:
•	❌ бизнес-логику бронирования 
•	❌ AI 
•	❌ фильтрацию туров 
Взаимодействует:
•	с Амирхоном (формат API) 
•	с Айбеком (логика бронирования) 
________________________________________
🔥 Айбек — Business Logic Owner (самый важный после архитектора)
Делает:
•	ЛОГИКА бронирования: 
o	проверка доступности 
o	контроль мест 
o	статусы (pending / confirmed) 
•	связь booking ↔ payment 
•	расчёт цены 
Отдаёт:
•	готовую логику для: 
o	POST /bookings 
o	POST /payments 
НЕ делает:
•	❌ модели 
•	❌ CRUD туров 
•	❌ UI/ответы “как красиво вернуть данные” 
Взаимодействует:
•	с Маратом (он реализует endpoints) 
•	с Элиной (если нужны изменения в моделях) 
________________________________________
🧩 Эвелина — Tours API
Делает:
•	GET /tours 
•	GET /tours/{id} 
•	фильтрация: 
o	цена 
o	регион 
o	сложность 
•	пагинация 
Отдаёт:
•	стабильный API для фронта 
НЕ делает:
•	❌ бронирование 
•	❌ оплату 
•	❌ auth 
Взаимодействует:
•	с Ариетом (фронт туров) 
________________________________________
👤 Марат — Users + Booking Endpoints
Делает:
•	GET /profile 
•	UPDATE /profile 
•	GET /bookings/my 
•	POST /bookings (НО!) 
❗ Важно:
•	использует ЛОГИКУ Айбека 
•	сам НЕ придумывает поведение 
НЕ делает:
•	❌ бизнес-логику бронирования 
•	❌ payments логику 
Взаимодействует:
•	с Айбеком (логика) 
•	с фронтом (через API) 

